# 2026-03-25 — FrameClaim Build Day

## Massive build day — Phases 2–4 completed and deployed

### Waterfall Plan Created
- Phases 1–6 defined for FrameClaim
- Phases 1–4 completed today, Phase 5 (launch) in progress, Phase 6 pending

### Phase 2 — Core Features (completed)
- Onboarding flow built (`/onboarding` page — 3-step wizard)
- Settings moved from localStorage to DB (displayName, address, phone, portfolio, onboardingDone fields)
- `prisma db push` issue: new columns added to schema but not pushed to Turso — fixed via direct ALTER TABLE on Turso
- Folder renamed: `shieldshot` → `frameclaim` locally

### Phase 3 — Hardening & QA (completed)
- Auth/ownership audit on all API routes
- Rate limiting on /api/contact, /api/reviews, /api/scan/guest
- Error handling pass (dashboard try/catch, stripe webhook, vision API)
- Welcome email + claim confirmation email added to lib/email.ts
- Mobile responsiveness + accessibility pass (aria-labels, focus rings, role=alert)

### Phase 4 — Beta Launch Prep (completed)
- Landing page copy refreshed (hero subheadline, features section)
- Terms & Privacy pages already complete
- Contact page functional
- Vercel Analytics added to layout.tsx
- SEO: sitemap.ts, robots.txt, og:image (og.svg), metadata
- @vercel/analytics installed

### Bug Fixes
- **Signup 500 error**: Turso DB missing new columns — fixed with ALTER TABLE directly on Turso
- **"Demo Photographer"**: hardcoded string → replaced with session name/email
- **Vision API false positives**: removed pagesWithMatchingImages (was returning visually similar pages, not actual copies)
- **Upload UX**: was redirecting to Alerts tab after every upload — fixed to stay on Images, only switch to Alerts if matches found
- **Image thumbnails in import**: CSP blocking external images — added `https:` to img-src directive

### New Features
- **Website URL import**: scrape portfolio site, pick photos from grid, bulk import (Pro only, up to 200)
- **Instagram import**: scrape public profile photos (best effort, Pro only)
- **Lightroom-style dedup**: already-imported photos greyed out + pre-deselected on re-import (sourceUrl field added to Image model)
- **Account deletion blacklist**: BlockedEmail table — deleted accounts can't re-register
- **Light/dark mode**: CSS variables + next-themes, toggle in dashboard sidebar + settings + landing nav
- **Storage optimization**: compress to 2000px/q85 on upload, resize to 1024px for Vision API, delete blobs on image delete

### Settings Page Redesign
- Tabbed layout: Profile / Domains / Integrations / Billing / Danger Zone
- Gradient avatar with initials, plan badge
- DB-backed profile fields

### Pricing + Landing Updates
- Pro tier: website import, Instagram import, Flickr sync all listed with "New" badges
- Free tier: 20 image limit, core features

### Scan Architecture Fix
- Vision API was running inline during upload → Vercel timeouts dropped ScanLog creation
- Fixed: upload creates image fast, scan runs separately (client calls /api/images/[id]/scan)
- Vision API confirmed working: 71 calls, 0 errors, ~865ms avg latency, still in free tier (1000/mo)

### Notifications / Telegram
- Telegram connected: user ID 8370238821
- Approved with: `openclaw pairing approve telegram 33T9CZHN`
- All future build notifications go to Telegram

### Cron Lessons Learned
- Cron agents must use `channel=telegram, target=8370238821` for notifications
- Never use "Cliff" as WA target — needs E.164 format or JID
- `channel=heartbeat` does not exist
- Canvas dashboard breaks when browser restarts between cron runs

### Users as of 2026-03-25
- saintstouch.photography@gmail.com — Cliff (admin/test, plan: pro)
- bob.medialab@gmail.com — likely test
- valmikinaina@gmail.com — Cliff's test account
- 12345@gmail.com — test
- kiranhere@gmail.com — **first real user** ✅
- testcheck999@example.com — automated test account

### Marketing Decision
- No paid ads yet — waiting for 10 real users and product-market fit signal
- Strategy: organic DMs to photographers, Reddit (r/photography), YouTube/newsletter outreach
- IG story posted, Product Hunt submitted

### DB Schema (Turso) — current columns added today
- User: displayName, address, phone, portfolio, onboardingDone
- Image: sourceUrl
- BlockedEmail: id, email, reason, blockedAt (new table)

### Misc
- `prisma.config.ts` datasource URL updated to prefer TURSO_DATABASE_URL over DATABASE_URL
- build script: `next build` only (prisma db push removed — manage schema via direct ALTER TABLE)
- serve server running on localhost:4242 for build dashboard HTML
