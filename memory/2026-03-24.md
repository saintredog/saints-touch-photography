# 2026-03-24 — Daily Notes

## FrameClaim — Stripe & Portal Fixes

### Stripe Checkout (SDK → raw fetch)
- Stripe SDK v20 had network connectivity issues on Vercel serverless (timeout/retry errors)
- Fixed: rewrote `/api/stripe/checkout/route.ts` to use raw `fetch` to `https://api.stripe.com/v1/checkout/sessions`
- Fixed: rewrote `/api/stripe/portal/route.ts` the same way — no SDK, no Prisma
- Portal now looks up Stripe customer by email via `/v1/customers/search` — avoids Prisma cold-start

### Monthly Price ID Was Stale in Vercel
- Production `STRIPE_PRICE_ID` was an old/wrong value
- Fix: re-added via `printf 'price_1TEFkz3oNH3Ff6rwln0woIAJ' | vercel env add STRIPE_PRICE_ID production --force`

### First Live Payment (Cliff's own account)
- User: `12345@gmail.com` | ID: `cmn51wimk000004ics1hqlgu3`
- Stripe customer: `cus_UD1u20D3t8AUiy`
- Webhook fired (`evt_1TEbqW3oNH3Ff6rwHfb3DZeA`) but DB wasn't updated (domain SSL propagation likely caused delivery failure)
- Manually updated via: `turso db shell shieldshot "UPDATE User SET plan='pro', stripeId='cus_UD1u20D3t8AUiy' WHERE id='cmn51wimk000004ics1hqlgu3'"`

### Security Review
- `?upgraded=true` URL param: only shows banner cosmetically; all Pro feature gates check `plan` state from `/api/user/plan` API — confirmed safe

### Status at EOD
- Both monthly and yearly checkout: ✅ working
- Manage Subscription portal: deployed fix, pending user confirmation
- Webhook delivery: should work going forward now domain is fully live
