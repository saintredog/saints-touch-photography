# 2026-03-23 — Daily Notes

## ShieldShot — Deployment & Production Work

### Status: Live at https://shieldshot.io (Vercel)
- GitHub repo: https://github.com/saintredog/shieldshot (private, user: saintredog)
- Vercel project: saintstouchphotography-3708s-projects/shieldshot

### Database: Turso (cloud SQLite)
- DB name: shieldshot | Account: saintredog
- URL: https://shieldshot-saintredog.aws-us-east-1.turso.io
- Auth token stored in Vercel env as TURSO_AUTH_TOKEN (no trailing newline)
- TURSO_DATABASE_URL must use https:// (not libsql://) for Vercel serverless
- **Lesson learned**: `vercel env add` via `<<< "value"` adds trailing newline — always use `printf 'value' | vercel env add` instead

### Admin Credentials
- Email: cliff@shieldshot.io | Password: Cliff2026! | plan: pro | id: cliff-admin
- Old admin (saintstouch.photography@gmail.com) also in DB but had corrupt hash — cliff@shieldshot.io is the working one

### Auth
- NextAuth v4 CredentialsProvider + bcryptjs (12 rounds)
- Sessions: JWT in encrypted cookies, 30-day default
- Signup page: /signup (free plan, 20 image limit)
- Login page: /login (has "Sign up free" link)
- Email verification: not implemented (skipped for now)

### Stripe Webhook
- Destination ID: we_1TEHXW3oNH3Ff6rw0D9DCYAT
- Endpoint: https://shieldshot.io/api/webhooks/stripe
- Secret: [ROTATED] (stored in Vercel, no trailing newline)
- Listening to: checkout.session.completed, customer.deleted
- Note: customer.subscription.deleted NOT added — may want to add later for cancellation handling

### Debug endpoint
- ~~Removed~~ — confirmed working, cleaned up in final deploy

### Key fixes made today
- Prisma libsql adapter: must pass { url, authToken } (Config), not a Client instance
- https:// URL required for Turso in serverless (libsql:// fails)
- All Vercel env vars re-added with printf to strip trailing newlines
- Confirmed: 2 users in Turso, direct connection verified (userCount: 2)

### Env vars in Vercel (production)
- TURSO_DATABASE_URL, TURSO_AUTH_TOKEN, NEXTAUTH_URL, NEXTAUTH_SECRET
- STRIPE_SECRET_KEY, STRIPE_PUBLISHABLE_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRICE_ID
- GOOGLE_VISION_API_KEY, CRON_SECRET, DATABASE_URL (legacy, may be unused)

### Local project path
- /Users/cliffordroberts/Documents/shieldshot/ (the real one — NOT /Documents/Claude Code/shieldshot/)
